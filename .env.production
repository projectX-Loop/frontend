# OCI/nginx production build: the browser calls the same origin only.
VITE_USE_MOCK=0
VITE_API_BASE=/api/v1
# RAG backend (explanation + question/answer) is merged and ready as of 2026-09-07.
VITE_ENABLE_RAG=true
