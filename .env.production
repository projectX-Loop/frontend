# OCI/nginx production build: the browser calls the same origin only.
VITE_USE_MOCK=0
VITE_API_BASE=/api/v1
# RAG is deployed only after its backend is available and explicitly enabled.
VITE_ENABLE_RAG=false
