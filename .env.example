# 개발: 목 데이터(예시 JSON)로만 동작. Spring 없이 화면 개발 가능
VITE_USE_MOCK=1
# 실서버: Spring 주소. vite dev 서버는 /api → 여기로 프록시 (vite.config.ts)
VITE_API_BASE=/api/v1
# RAG is opt-in; keep it disabled until the backend endpoint is deployed.
VITE_ENABLE_RAG=false
